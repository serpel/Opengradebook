

<div class="label-field-pair">
  <label for="student_grade"><%= t('cc') %></label>
  <div class ="scrolable" >
    <% x = '<span> x </span>' %>
    <div class="scroll-inside">
<% @copies.each do |r|  %>
  <div class="hover">
    <%=  %> <%= link_to r.full_name + x, "#", :onClick => "remove_copy(#{r.id})", :class=> 'individual',
                        :before => "Element.show('loader')",
                        :success => "Element.hide('loader')" %>
  </div>
<% end %>
    </div>
  </div>
</div>
<div class="extender"></div>
