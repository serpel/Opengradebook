<div id="content-header">
  <%= image_tag("/images/show_settings.png") %>
  <h1><%= t('configuration_text') %></h1>
  <h3><%= t('configuration_text') %> <%= t('home') %></h3>
  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png",:border => 0), "history.back()" %>
  </div>
</div>

<div id="page-yield">

  <% unless flash[:notice].nil? %>
      <p class="flash-msg"> <%= flash[:notice] %> </p>
  <% end %>

  <div id="box">

    <%# if permitted_to? :index, :courses %>

    <div class="link-box">
      <div class="link-heading"><%= link_to "#{t('gradebook_subjects')}", {:controller => "employee", :action => "get_subjects"} %></div>
    </div>

    <div class="link-box">
      <div class="link-heading"><%= link_to "#{t('print_report')}", {:controller => 'school_reports', :action => 'batches', :id => 1}  %></div>
    </div>

    <%# end %>


    <div class="link-box">
      <div class="link-heading"><%= link_to "#{t('gradebook_personality')}", {:controller => "student_grade_personalities",
                                                                              :action => "batches",
                                                                              :id => 1} %></div>
    </div>

  </div>
</div>