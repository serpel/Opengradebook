

<div class="box">
  <% form_for :student_school, :url => {:action => 'schools'} do |f| %>
    <div class="label-field-pair">
      <label><%= t('enter_new_school') %> : </label>
      <div class="text-input-bg"><%= f.text_field :school_name, :value => '' %>
      </div>
    </div>
    <%= submit_tag "", :value => "► #{t('save')}", :class => "submit_button" %>
  <% end %>
</div>
